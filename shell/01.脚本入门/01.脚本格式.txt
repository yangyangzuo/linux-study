脚本可以#!/bin/bash开头  (指定命令解释器)

可以手动指定任意的解析器,如果不指定,则使用系统默认的命令解释器
#!/bin/bash
#!/bin/sh
.....



执行脚本:
    1.通过shell去调用*.sh文件
        sh *.sh
        bash *.sh
        ......
    2.直接执行*.sh文件,*.sh需要赋予执行权限
        chmod 777 *.sh
        ./*.sh